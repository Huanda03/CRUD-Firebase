# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CRUD Firebase' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CRUD Firebase

  target 'CRUD FirebaseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CRUD FirebaseUITests' do
    # Pods for testing
	pod 'Firebase/Core'
	pod 'Firebase/Firestore'
  end

end
